# abv-bc-session1-oop
A Github repo containing a real-world problem modeled in javascript using OOP.

Run `npm install --save-dev babel-cli` to get started. Follow through with complete babel installation.